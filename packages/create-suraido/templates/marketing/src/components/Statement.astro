---
import Slide from "suraido/components/Slide.astro";
interface Props { title?: string; number?: boolean }
const { title, number = false } = Astro.props;
---

<Slide title={title} number={number}><p class="statement"><slot /></p></Slide>
