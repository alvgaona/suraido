---
interface Props {
  value: string | number;
  label: string;
}
const { value, label } = Astro.props;
---

<div class="deck-stat">
  <span class="deck-stat__num">{value}</span>
  <span class="deck-stat__label">{label}</span>
</div>
