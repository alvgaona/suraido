---
// Wraps step children (<Step>) that reveal one at a time. The reveal is driven
// by the runtime in a later phase; children render visible until then.
---

<div class="deck-steps"><slot /></div>
